type Blouse {
  title: String!
  _id: ID!
  deadline: String!
  createdAt: String!
  updatedAt: String!
}

input BlouseInput {
  title: String!
  deadline: String!
}

type Query {
  blouses: [Blouse]!
}

type Mutation {
  addBlouse(blouseInput: BlouseInput!): Blouse
}
